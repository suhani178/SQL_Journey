-- Day 15 Practice Queries

-- Q1
SELECT ENAME
FROM EMPLOYEE E
WHERE SAL >
(
SELECT AVG(SAL)
FROM EMPLOYEE
WHERE DEPTNO = E.DEPTNO
);

-- Q2
SELECT ENAME
FROM EMPLOYEE E
WHERE SAL <
(
SELECT AVG(SAL)
FROM EMPLOYEE
WHERE DEPTNO = E.DEPTNO
);

-- Q3
SELECT ENAME
FROM EMPLOYEE E
WHERE SAL =
(
SELECT MAX(SAL)
FROM EMPLOYEE
WHERE DEPTNO = E.DEPTNO
);

-- Q4
SELECT ENAME
FROM EMPLOYEE E
WHERE SAL =
(
SELECT MIN(SAL)
FROM EMPLOYEE
WHERE DEPTNO = E.DEPTNO
);

-- Q5
SELECT ENAME
FROM EMPLOYEE E
WHERE SAL =
(
SELECT AVG(SAL)
FROM EMPLOYEE
WHERE DEPTNO = E.DEPTNO
);

-- Q6
SELECT DNAME
FROM DEPARTMENT D
WHERE EXISTS
(
SELECT *
FROM EMPLOYEE E
WHERE E.DEPTNO = D.DEPTNO
);

-- Q7
SELECT DNAME
FROM DEPARTMENT D
WHERE NOT EXISTS
(
SELECT *
FROM EMPLOYEE E
WHERE E.DEPTNO = D.DEPTNO
);

-- Q8
SELECT ENAME
FROM EMPLOYEE E
WHERE EXISTS
(
SELECT *
FROM DEPARTMENT D
WHERE E.DEPTNO = D.DEPTNO
);

-- Q9
SELECT ENAME
FROM EMPLOYEE E
WHERE NOT EXISTS
(
SELECT *
FROM DEPARTMENT D
WHERE E.DEPTNO = D.DEPTNO
);

-- Q10
SELECT DNAME
FROM DEPARTMENT D
WHERE EXISTS
(
SELECT *
FROM EMPLOYEE E
WHERE E.DEPTNO = D.DEPTNO
);
