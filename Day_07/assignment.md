Day 7 Assignment

Q1

SELECT COUNT(*)
FROM EMP;

Q2

SELECT SUM(SAL)
FROM EMP;

Q3

SELECT AVG(SAL)
FROM EMP;

Q4

SELECT MAX(SAL)
FROM EMP;

Q5

SELECT MIN(SAL)
FROM EMP;

Q6

SELECT COUNT(*)
FROM EMP
WHERE DEPTNO = 20;

Q7

SELECT SUM(SAL)
FROM EMP
WHERE DEPTNO = 30;

Q8

SELECT AVG(SAL)
FROM EMP
WHERE DEPTNO = 10;

Q9

SELECT MAX(SAL)
FROM EMP
WHERE JOB = 'CLERK';

Q10

SELECT MIN(SAL)
FROM EMP
WHERE JOB = 'MANAGER';

Q11

SELECT COUNT(*)
FROM EMP
WHERE SAL > 2000;

Q12

SELECT AVG(SAL)
FROM EMP
WHERE JOB = 'ANALYST';

Q13

SELECT MAX(EMPNO)
FROM EMP;

Q14

SELECT MIN(DEPTNO)
FROM EMP;

Q15

SELECT MAX(SAL) - MIN(SAL)
FROM EMP;
