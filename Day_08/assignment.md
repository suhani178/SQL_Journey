Day 8 Assignment

Q1

SELECT DEPTNO, SUM(SAL)
FROM EMP
GROUP BY DEPTNO;

Q2

SELECT DEPTNO, AVG(SAL)
FROM EMP
GROUP BY DEPTNO;

Q3

SELECT DEPTNO, MAX(SAL)
FROM EMP
GROUP BY DEPTNO;

Q4

SELECT DEPTNO, MIN(SAL)
FROM EMP
GROUP BY DEPTNO;

Q5

SELECT DEPTNO, COUNT(*)
FROM EMP
GROUP BY DEPTNO;

Q6

SELECT JOB, AVG(SAL)
FROM EMP
GROUP BY JOB;

Q7

SELECT JOB, COUNT(*)
FROM EMP
GROUP BY JOB;

Q8

SELECT JOB, SUM(SAL)
FROM EMP
GROUP BY JOB;

Q9

SELECT JOB, MAX(SAL)
FROM EMP
GROUP BY JOB;

Q10

SELECT JOB, MIN(SAL)
FROM EMP
GROUP BY JOB;

Q11

SELECT DEPTNO, AVG(SAL)
FROM EMP
WHERE SAL > 1000
GROUP BY DEPTNO;

Q12

SELECT DEPTNO, COUNT(*)
FROM EMP
WHERE SAL > 1500
GROUP BY DEPTNO;

Q13

SELECT DEPTNO, SUM(SAL)
FROM EMP
WHERE DEPTNO = 20
GROUP BY DEPTNO;

Q14

SELECT JOB, MAX(SAL)
FROM EMP
WHERE JOB = 'MANAGER'
OR JOB = 'CLERK'
GROUP BY JOB;

Q15

SELECT DEPTNO, MIN(SAL)
FROM EMP
WHERE DEPTNO = 10
OR DEPTNO = 20
GROUP BY DEPTNO;

