-- Day 8 Practice

SELECT DEPTNO, SUM(SAL)
FROM EMP
GROUP BY DEPTNO;

SELECT DEPTNO, AVG(SAL)
FROM EMP
GROUP BY DEPTNO;

SELECT DEPTNO, MAX(SAL)
FROM EMP
GROUP BY DEPTNO;

SELECT DEPTNO, MIN(SAL)
FROM EMP
GROUP BY DEPTNO;

SELECT DEPTNO, COUNT(*)
FROM EMP
GROUP BY DEPTNO;

SELECT JOB, AVG(SAL)
FROM EMP
GROUP BY JOB;

SELECT JOB, COUNT(*)
FROM EMP
GROUP BY JOB;

SELECT JOB, SUM(SAL)
FROM EMP
GROUP BY JOB;

SELECT JOB, MAX(SAL)
FROM EMP
GROUP BY JOB;

SELECT JOB, MIN(SAL)
FROM EMP
GROUP BY JOB;
